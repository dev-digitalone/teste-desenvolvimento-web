# How to run?

## Installation 
install dependencies
`composer install`. 

`docker-compose up`.
or if you are in RHEL based OS run:
`podman-compose up`.

then
`php spark serve`.

## Setup
Copy `env` to `.env`.


